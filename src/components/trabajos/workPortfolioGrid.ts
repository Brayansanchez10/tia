/** Clases del grid del listado de trabajos (reutilizable sin mezclar con el componente). */
export function workPortfolioGridClass(): string {
  return 'm-0 grid list-none grid-cols-1 gap-8 p-0 sm:gap-10 md:gap-12'
}
